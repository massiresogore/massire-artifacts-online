spring:
  profiles:
    active: dev  # By default, the active profile is "dev". During production, it will be replaced to "prod" due to Azure's environment variables ok daccord .

  web:
    resources:
      add-mappings: false
#  cloud:
#    azure:
#      compatibility-verifier:
#        enabled: false
api:
  endpoint:
    base-url: /api/v1
server:
  port: 8080


